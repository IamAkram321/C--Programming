#include<stdio.h>

int main()
{
    // This is using hard coded inputs
    int a=34;
    int b=2;
    // This is using  input supplied by the user!!!
    // int a;
    // int b;
    // printf("Enter the value of a\n");
    // scanf("%d", &a);
    // printf("Enter the value of b\n");
    // scanf("%d", &b);
    if(a>7 && b<4)
    {
        printf("Yes! a is greater than 7 and b is less than 4");
    }
    // else{
    //     printf("No! a is not greater than 7 and b is not less than 4");
    // }
    return 0;
}